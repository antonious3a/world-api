server:
  port: 9088

spring:
  application:
    name: world-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/world?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: antonio3a
    password: Antonio.3A
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        sql: debug

springdoc:
  swagger-ui:
    use-root-path: true
    tags-sorter: alpha
    operations-sorter: alpha
    displayRequestDuration: true

keycloak:
  realm: antonio3a-realm
  auth-server-url: http://localhost:9180/auth
  ssl-required: external
  resource: antonio3a-world-api
  credentials:
    secret: Xi1hFrWjokjqvFIv5qOiUg9wjrAfbiKQ
  use-resource-role-mappings: true
  bearer-only: true